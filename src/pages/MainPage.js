import React from 'react';
import Button from '../components/common/Button';

const MainPage = () => {
  return (
    <div>
      메인페이지
      <div>
        <Button>버튼</Button>
      </div>
    </div>
  );
};

export default MainPage;
